# This repo ships ONLY the self-contained deployed tool + docs.
# Ignore everything by default, then re-include the deploy artifacts.
*
!.gitignore
!/index.html
!/tool.html
!/news.html
!/sitemap.xml
!/robots.txt
!/posts/
!/posts/*.html
!/README.md
!/.nojekyll
